export class Users {
    key: string;
    fullname: string;
    email: string;
    foto: string;
    lat: number;
    lng: number;
    constructor(key: string, fullname: string, email: string, foto:string, lat:number, lng:number){}
}